#include <stdio.h>

/**
 * main- Entry point of the program
 * description: print all single digits of base 10
 *
 * Return: 0 if successful
 */

int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d\n", i);
	return (0);

}

