#include "main.h"
/**
 * mul-  a function that multiplies two integers.
 * @a: parameter for check
 * @b: parameter for check
 *
 * Return: 0 if successful
 */

int mul(int a, int b)
{
	return ((a * b));
}
