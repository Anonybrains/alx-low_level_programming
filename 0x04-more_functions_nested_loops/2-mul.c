#include "main.h"
/**
 * mul- the function that multiplies two integers.
 * @a: the parameter that is multiplied
 * @b: the parameter that is multiplied
 *
 * Return: the product if successful
 */

int mul(int a, int b)
{
	return ((a * b));
}
